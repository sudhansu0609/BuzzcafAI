# Buzzcaf Studio — Progress Log

Running record of what has actually landed, newest first. Plan: `PLAN.md`.
A row is **done** only with the evidence that proves it. Nothing is marked done on intent.

## 2026-09-17 — v11 BuzzEdit video-production bridge

Full plan: `C:\Users\singh\.claude\plans\buzzcafai-has-agents-and-velvety-fog.md`
(+ its line-referenced verification file). Servers, ComfyUI and BuzzEdit were
never started this session (guardrail); everything below is import/syntax/
type-check evidence, not a live end-to-end render. That live run (item under
"Not verified" below) is still owed.

| Item | Status | Evidence |
|---|---|---|
| v11.1 `integrations/buzzedit_client.py` | ✅ done (import-checked) | New file; `python -c "from integrations.buzzedit_client import BuzzEditClient, BuzzEditUnavailable"` succeeds; `base_url()` uses `buzzcaf_ports.discover("buzzedit", 8099, "/api/health")` |
| v11.2 `integrations/buzzedit_script.py` | ✅ done (import-checked) | New file; `to_directive_script(script_text, visual_plan) -> (annotated_text, skipped_beats)`; exact-substring then sentence-level `difflib` fallback; `]` sanitised out of every directive argument; a local self-check regex counts inserted directives/headings and logs (not raises) on a mismatch |
| v11.3 `integrations/buzzedit_settings.py` | ✅ done (import-checked) | New file; `settings_for_brand(brand, overrides)`; brand→genre map matches the source plan; overrides merged last |
| v11.4 `runtime/workflow.py` visual_plan routing + JSON instruction | ✅ done | `VISUAL_PLAN_JSON_INSTRUCTION` constant added (mirrors `RESEARCH_JSON_INSTRUCTION`) and injected into the prompt for `output_asset_type == "visual_plan"`; new dedicated `elif` writes `production/visual_plan.json` (was previously falling into the bare-root `else`). `python -c "from core.workflow import workflow_registry; ..."` → all 6 workflows still load |
| v11.5 `prompts/agents/PromptEngineer.md` | ✅ done | Frontmatter `version: 1.1.0`, `outputs` gains `visual_plan`; §8/§25/§26/§27/§28 extended with the `{genre, beats:[...]}` schema and worked examples, old `ai_prompt_dossier` schema kept |
| v11.6 "Visual Plan" workflow step | ✅ done | Added to `beyond3baje_documentary.json`, `after_dark_narration.json`, `khayal3baje_horror.json`, `life3baje_video.json`, `VideoProductionWorkflow.json` (right after each one's `script`-producing step); intentionally **not** added to `spilled_coffee_story.json` (it produces `story`/`outline`/`script_final`, never `script`, and is a text/audiobook workflow, not a BuzzEdit candidate). `python -c "from core.workflow import workflow_registry; print(len(workflow_registry.workflows))"` → `6`, no validation errors |
| v11.7 `app/api/produce_api.py` + mount | ✅ done | New router: `POST /api/projects/{id}/produce_video` (background `threading.Thread`, module-level `_produce_status` dict + lock, added to `active_executions`), `GET .../produce_video/status`. Mounted in `app/main.py` (`from app.api.produce_api import router as produce_router` + `app.include_router(produce_router)`). Verified via `app.openapi()["paths"]`: `/api/projects/{project_id}/produce_video`, `/api/projects/{project_id}/produce_video/status`, `/api/produce/plan` all present. Every symbol the module imports (`event_bus`, `Project`, `BuzzEditClient`/`BuzzEditUnavailable`, `to_directive_script`, `settings_for_brand`, `LLMService`/`clean_json_response`, `AgentFactory`, `VISUAL_PLAN_JSON_INSTRUCTION`, `sentinel_client`, `app.main.active_executions`) individually import-checked by name |
| v11.7b SSE event types | ✅ done | `app/services/events.py` `EVENT_TYPES` gains `video_progress`, `video_ready`, `video_failed` |
| v11.8 `POST /api/produce/plan` (reverse endpoint) | ✅ done | Same router as v11.7; synchronous, runs the `PromptEngineer` visual-plan agent + `to_directive_script` + `settings_for_brand`; route present in `app.openapi()["paths"]` |
| v11.9 config/docs | ✅ done | `backend/config/config.example.json` gains `buzzedit_url`; `.env.example` gains a `BUZZEDIT_URL` example under a new "Sibling apps" section; `ECOSYSTEM.md` gains §7a "BuzzEdit bridge (produce_video)" documenting both routes + the interchange contract, and the 3 new SSE names added to §7's event list. `dexter/ECOSYSTEM.md` is now stale until re-synced (same accepted pattern as v9/v10 per this file) |
| v11.10 Frontend (`Projects.tsx`, `lib/types.ts`) | ✅ done (type-checked) | `lib/types.ts` gains `ProduceVideoStatus`. `pages/Projects.tsx`: per-project "Produce Video" block (gated on `assets.script && assets.visual_plan`) with an absolute-path recording input, a genre/coverage style-override mini-form prefilled from the brand, a "Produce Video" button, a progress bar polling `GET .../produce_video/status` every 3s, an output-path display (flagged as a BuzzEdit-local path, not a link — no proxy exists), and a full-screen read-only Teleprompter overlay for the `buzzedit_script` asset. `cd frontend && ./node_modules/.bin/tsc --noEmit` → **0 errors**. `npm run build` was **not** run (task instruction); `backend/app/static/` is stale relative to this change |
| v11.11 BuzzEdit `backend/integrations/buzzcaf_client.py` | ✅ done (import-checked, BuzzEdit repo) | New file + new `backend/integrations/__init__.py` package (didn't exist before). `discover("buzzcaf", 8099, "/health")` + `plan_visuals(transcript, brand)` → `POST /api/produce/plan`. `PYTHONPATH=. python -c "from integrations.buzzcaf_client import base_url, plan_visuals"` succeeds |
| v11.12 BuzzEdit `POST /api/agents/plan_visuals` + AgentPanel UI | ✅ done (import-checked + type-checked, BuzzEdit repo) | `routes/agents.py` gains `plan_visuals` (transcribes-or-uses-existing-script → `buzzcaf_client.plan_visuals` → `presentation.script.apply_project_script`, all server-side); route confirmed at `/api/agents/plan_visuals` via both direct router inspection and `app.openapi()["paths"]` on the full `main.py` app. `src/hooks/api.ts` gains `planVisualsWithBuzzcaf` + `BUZZCAF_BRAND_OPTIONS`. `src/components/AgentPanel.tsx` gains a brand `<select>` + "Plan visuals with BuzzcafAI" button next to the script textarea, gated on `project?.transcript?.length`, using the file's existing `setError` pattern. `./node_modules/.bin/tsc --noEmit` (BuzzEdit repo) → **0 errors** |

**Not verified (owed, needs both apps + ComfyUI actually running):** a live
`produce_video` run end-to-end (Flow S and Flow R), the failure path with
ComfyUI stopped, a live `discover("buzzedit", ...)` resolving to a real
BuzzEdit process, and a live `POST /api/agents/plan_visuals` round trip on
BuzzEdit's side. All backend routes are import-clean and appear in each app's
own OpenAPI schema; none has been exercised against a running counterpart
process. Per the task's guardrails, no server/ComfyUI was started and no
tests were written this session.

## Status at a glance

| Item | Status | Evidence |
|---|---|---|
| 0.1 commit + `pre-v5` tag | ✅ done | 4 commits `a8bf90c..a32e0a3`, `git tag -l` → `pre-v5` (2026-09-06) |
| 0.3 PLAN.md / PROGRESS.md | ✅ done | this file |
| 0.4 ECOSYSTEM.md identical in both repos | ✅ done | `diff ECOSYSTEM.md ../dexter/ECOSYSTEM.md` empty |
| 0.5 test sandbox conftest | ✅ done | `pytest tests -q` → 136 passed; `md5sum backend/knowledge/*_memory.json` identical before/after; `main.py` now lists projects via `core.paths.PROJECTS_DIR` |
| 2.1 build into static mount | ✅ done | `vite.config.ts` `build.outDir: ../backend/app/static`; `npm run build` → `backend/app/static/index.html` refreshed (2026-09-06) |
| 2.2 desktop shell (**I2**) | ✅ done | `pythonw backend/desktop_app.py`: no new `conhost` (20 → 20), `/health` → `app:"buzzcaf"` after **4.0 s**, log `Window opening after 3.7s`, `<title>Buzzcaf Studio</title>` served at `/`, projects listed; attaches to an already-running backend instead of double-starting; `Buzzcaf Studio.vbs` + `start_buzzcafai.bat --dev`; `preflight.py` 7 ok |
| 2.3 voice removed | ✅ done | `voice_intent.py`, `voice_engine.py`, `/api/jarvis/voice`, `/api/voice/config`, clone routes, dictation tab, jarvis handlers, mic buttons gone; `grep -rni jarvis\|voice_intent\|webkitSpeech\|voice_engine backend/app frontend/src` empty; `pytest` 136 passed |
| 2.4 chat correctness | ✅ done | `services/studio_chat.py` builds persona (+roster once) + brand guide + ≤6 *relevant* memories + last 8 turns as messages; `LLMService.generate_chat`; `BaseAgent.execute_messages` (no Hinglish for chat, kept for workflow steps); `memory.retrieve(query=)`; full replies stored; 502 on failure; `tests/test_studio_chat.py` 9 tests, suite 145 passed |
| 2.5 chat is the front door (**I11**) | ✅ done | `App.tsx` rewritten as a shell over `src/pages/*` + `state/studio.tsx`; landing tab `studio_chat`; all requests via `apiFetch`/`services/api.ts`; `Toast`/`ConfirmDialog` replace 10 `alert()`/`confirm()`; no fabricated projects/topics; mock tabs removed; real `/health` badge; `tsc` + `vite build` green |
| 3.1 control API | ✅ done | `api/studio_api.py`: `GET /api/studio/state`, `POST /api/studio/chat`, `GET /api/studio/events` (SSE); `POST /api/projects/{id}/approve` (409 when nothing waits); events `project_created`/`step_started`/`step_completed`/`approval_needed`/`step_failed` published from create/execute; `/health` carries `app`+`version`; `tests/test_studio_api.py` 8 tests, suite 153 passed |
| 5.1 BuzzBrain store | ✅ done | `api/buzzbrain_api.py`: `POST /api/buzzbrain/snapshot` → `knowledge/buzzbrain/snapshots.jsonl` + `index.json`, `GET /latest`, `/channels`, `/videos/{id}`; `mine` only via `owner_channel_ids` setting or a brand name; `buzzbrain_snapshot` event; `tests/test_buzzbrain_api.py` 6 tests, suite 159 passed. BuzzBrain 1.2.0 pushes snapshots and queues offline |
| 5.4 frontend split + stub purge | ✅ done | frontend: 96 stub files deleted, `router.tsx` gone, `App.tsx` 3595 → 193 lines over `src/pages/*`; backend: 26 dead trees + 32 stub files removed (21 placeholder tests), auth stub unmounted, `app/db` + `alembic.ini` dropped (no code opened a database); tracked files 766 → ~530, Python files 243 → 86; `docs/` → `docs/legacy/` (185 files) with a pointer README; `pytest` 137 passed; ARCHITECTURE.md rewritten to the surviving tree |
| 5.5 streaming (optional) | ⬜ open | only if chat still feels slow with a local server up |
| P1 Studio off port 8000 + neighbours discover it (owner's ask 2026-09-09: CaliberAI on 8000 was mistaken for the Studio) | ✅ done | Default `BUZZCAF_PORT` 8000 → **8099** everywhere (`desktop_app.py`, `preflight.py`, `vite.config.ts`, `apps/cli/main.py`, `.env`, `.env.example`, ECOSYSTEM/README/ARCHITECTURE). `desktop_app._resolve_port`: attaches to a running Studio found on the preferred port, in `backend/config/studio.runtime.json`, or on the way forward; steps to the next free port when an unrelated process holds it; `_write_runtime`/`_clear_runtime` (pid-guarded, `atexit`). `.env` loaded early; Vite child gets the real port in its env. Dexter `buzzcaf_client.health_check` follows the runtime file / configured URL, only to a port whose `/health` says `app:"buzzcaf"`. BuzzBrain 1.3.0: host permission `http://127.0.0.1/*`, `findStudio()` scans 8099–8119, retries a failed snapshot once after re-finding. Live with CaliberAI on 8000: `_resolve_port(8000)` → 8001; backend up on 8099, runtime file written, second launch → `(8099, attached=True)`, Dexter client configured for 8000 → `online=True, base_url http://127.0.0.1:8099`, file cleared on exit. `pytest` Studio 169 passed, Dexter 395 passed; `node --check` + manifest JSON OK; `diff ECOSYSTEM.md ../dexter/ECOSYSTEM.md` empty. Owner: reload BuzzBrain in Chrome |
| P2 `--dev` window never shows another project's Vite (owner's report 2026-09-09: `start_buzzcafai.bat` opened CaliberAI — its Vite was on `::1:5173`, ours had drifted to 5174, the window opened `localhost:5173`) | ✅ done | `desktop_app.py`: `DEV_PORT` (`BUZZCAF_DEV_PORT`, default 5173) → `_resolve_dev_port` picks a free port; `_start_vite_dev(port, dev_port)` pins Vite with `--port N --strictPort` and passes both ports in the child env; the window opens only when `wait_for(f"{dev_url}/health")` — through Vite's proxy — answers `app:"buzzcaf"`; URL uses `127.0.0.1`, not `localhost`. `_port_is_free` now probes `127.0.0.1` **and** `::1` with a connect and binds with `SO_EXCLUSIVEADDRUSE` (a Windows wildcard/IPv6 listener let a loopback bind "succeed"). `_stop_process_tree(proc, port)`: psutil kills the npm→cmd→node tree and anything still listening on our dev port (three orphaned Vites were found holding 5174/5175/5180 after earlier launches); `psutil` added to `requirements.txt`. `vite.config.ts` `strictPort: true`, port from `BUZZCAF_DEV_PORT`. Live with CaliberAI's Vite on 5173: `_studio_answering(5173)` → False, fresh Vite on a free port → `/health` via proxy `200 {"app":"buzzcaf"}`, `_studio_answering` via proxy True; orphan on 5180 freed by the port path, live Vite on 5181 freed by the tree path; `pytest` 169 passed, `tsc` + `vite build` green |
| v9 A1 validator reads the registry | ✅ done | `core/workflow.py` `validate` now builds `valid_agents = set(agent_registry.agents)` (was a six-name literal at `:73`); `tests/test_core.py` gains `test_workflow_validator_reads_the_agent_registry` (a `ScriptWriter` step registers, `NoSuchAgent` raises `ValueError: ... not in the Agent Registry`) and `test_shipped_workflows_all_load` (6 workflows, all with steps); suite 144 passed |
| v9 A2 `/api/agents` carries frontmatter; Personas by department | ✅ done | `main.py` `get_all_agents` reads `agent_registry.agents.values()`, `?department=` filter, `status: "registered"` (the invented `"Active & Idle"` is gone). `GET /api/agents` → `count 105`, 0 rows with an empty `department` or `role`, 13 departments; `?department=Research` → 16. `pages/Workforce.tsx` groups by department with per-group counts and filters on name **or** department. (The plan's "19 groups" was written pre-A4; after the A4 merge the registry holds 13.) |
| v9 A3 any persona in chat | ✅ done | `StudioChat.tsx` persona `<select>` fed by `/api/agents`, `<optgroup>` per department, default = `strategistFor(channel)`, choice stored at `buzzcaf_chat_agent:<channel>` in `localStorage`, "back to the strategist" reset; each reply bubble is headed by `data.agent_name`. `POST /api/topics/agent_chat {agent_name: "FactChecker", channel: "Beyond3Baje"}` → HTTP 200, `agent_name: "FactChecker"`, `backend/knowledge/agent_factchecker_memory.json` created, and with a real model up (`simulated: false`) the answer was in that persona's voice: *"To verify a claim about a fort guard vanishing in 1923, I would first check historical records and archives from that period, focusing on local newspapers, military records, and any available eyewitness accounts…"* |
| v9 A4 duplicate personas pruned | ✅ done | 10 files removed (`CTRAnalyzer`, `RetentionAnalyzer`, `RecommendationEngine`, `TrendAnalyzer`, `TrendResearcher`, `Editor`, `ThumbnailPlanner`, `SEOSpecialist`, `PublishingScheduleManager`, `SchedulerAgent`); the three renamed survivors keep the better prose (`EditorAgent` ← `Editor`'s 29-part spec, `SEOManagerAgent` ← `SEOSpecialist`, `ScheduleManager` ← `PublishingScheduleManager`) and are versioned `1.1.0`. `"X Department"` folded into `"X"`, `ProjectManagerAgent` moved to `Project Management` → **115 → 105 personas, 19 → 13 departments** (the 12 classes + `Creative`). 12 files had `dependencies:`/prose references rewritten; `python -c "from core.agent import agent_registry"` logs no `unresolved dependency` line. `preflight.py` `EXPECTED_PERSONAS = 105`; `core/base_agent.py` roster counts now come from the registry instead of the hard-coded "115 / 19"; department classes, `tests/test_all_79_agents.py` and `scripts/verify_agent_flow.py` renamed to the canonical names |
| v9 C1 asset viewer | ✅ done | `components/Markdown.tsx` (105 lines, no library: headings, ordered/unordered lists, paragraphs, fenced code, inline `**bold**`/`*em*`/`` `code` ``; builds React elements, never `dangerouslySetInnerHTML`). `pages/Projects.tsx` lists `project.assets` as chips in a right-hand panel and renders `GET /api/projects/{id}/asset/{name}`; a `.json` asset arrives parsed and is shown as a fenced block. Live read: `GET /api/projects/2026-07-22_Beyond3Baje_TestLiveLogger/asset/script` → 200, `{"content": "Since the task requires a full script draft…"}` |
| v9 C2 workflow step preview + timeline | ✅ done | `pages/Dashboard.tsx` renders the chosen workflow's `steps[]` (number, name, `agent_role`, `· approval`) under the new-project form — `beyond3baje_documentary` shows its **13** steps (Research → Analytics), 4 of them approval steps. `pages/Projects.tsx` panel shows the same steps as a timeline coloured `done / current / waiting / failed / pending` from `steps_history` + `current_step`, approval steps marked. `WorkflowStepInfo` replaces the `steps?: unknown[]` placeholder in `lib/types.ts` |
| v9 C3 real approve | ✅ done | "Approve & continue" now posts `/api/projects/{id}/approve` (409 → toast "…Refreshing the list."); "Revise with notes" still posts `/execute` with `feedback`. `tests/test_studio_api.py::test_create_publishes_and_approve_advances_a_paused_step` gains `step_started == 2` (one frame for the run, one for the approve); suite 144 passed |
| v9 D1 research output real or labelled | ✅ done | `runtime/workflow.py`: the research step now asks for JSON (`RESEARCH_JSON_INSTRUCTION`, `require_json=True`) and writes one file per key. JSON reply → `timeline.md`, `facts.md`, `sources.md`, `media.md`, `unanswered_questions.md` plus the assembled `research/research.md` (the asset downstream steps read); prose reply → `research/research.md` only, the five section files are **not** created and `StepExecution.simulated_sections` lists all five. `grep -rn "Information gathered from research" backend` → no match (the literal is gone from the code, and the 10 stale filler files two old projects were carrying were deleted; their real `summary.md` is untouched). `tests/test_studio.py::test_research_step_writes_a_file_per_json_section` + `::test_research_step_labels_a_prose_reply_instead_of_faking_sections` |
| v9 D2 discover uses a model | ✅ done | `POST /api/topics/discover` runs the channel's strategist (`AgentFactory` + `engine.llm_service`, `require_json=True`) with the brand guide and the already-saved titles as context, and only falls back to the seed files when the reply was `last_response_simulated` or off-schema. Response carries `source: "model" \| "curated_seed" \| "curated_seed_fallback"`; the fabricated `inventory_counts` block (`raw_ideas: 120` etc., invented and unused) is gone. `TopicVault.tsx` shows a green "Written by <Strategist>" badge or an amber "⚠ Seed list, no model" one. `tests/test_studio_api.py::test_discover_returns_model_topics_when_a_provider_answers` + `::test_discover_falls_back_to_the_labelled_seed_list` |
| v9 D3 `AgentRegistry.json` removed | ✅ done | `backend/prompts/standards/AgentRegistry.json` deleted (nothing imported or opened it); `grep -rn AgentRegistry.json backend docs *.md` now matches only `PLAN.md`/`PROGRESS.md`. `AGENTS.md` keeps the line that `permissions` are "Declarative labels — **not enforced by any code**" |
| v9 B1 structured invocations + `agent_invoked` event | ✅ done | `process_agent_invocations(text, llm_service, caller)` now returns `(text, [{agent, task, output, simulated}])`, takes the caller's `LLMService` instead of building a new one, caps at `MAX_INVOCATIONS = 3` per reply (the rest come back `skipped: True` and are logged, not run) and strips tags from the delegated output so depth stays at 1. `run_chat` returns `invocations` and saves each specialist's work under its own name tagged `delegated`; every honoured invocation publishes `agent_invoked {agent, task_preview, simulated}` (added to `events.EVENT_TYPES`). `tests/test_studio_chat.py::test_two_invocations_produce_two_records_and_two_frames` (2 records, 2 frames, memory row tagged `delegated`) + `::test_a_fourth_invocation_is_skipped_not_run` (4 records, 3 run, 3 frames, 1 skipped) |
| v9 B2 invocation cards | ✅ done | `StudioChat.tsx` `InvocationCard`: "Delegated to <Agent>", the task, a "Show what it said" toggle rendering the output through `components/Markdown`, plus `⚠ SIMULATED` / `SKIPPED — over the per-reply limit` / `FAILED` badges. Cards hang under the reply that made them; `Invocation` typed in `lib/types.ts`. `tsc` + `vite build` green |
| v9 B3 delegation inside workflow steps | ✅ done | `runtime/workflow.py` `_apply_step_delegations` runs the same pass on a step's output (skipped for JSON assets, where a Markdown heading would break the parse), appends `## Delegated: <Agent>` sections to the asset and records `StepExecution.delegations` + a log line per delegation. `tests/test_studio.py::test_workflow_step_delegation_lands_in_the_asset`: the documentary's Scripting step delegates to `FactChecker`, `script/draft.md` carries `## Delegated: FactChecker` and the specialist's text, no `[INVOKE_AGENT` survives, history records `{"agent": "FactChecker", "task_preview": "Verify the 1923 dates.", "simulated": false, "skipped": false}`. Suite 151 passed |
| v9 E1 departments API | ✅ done | `Department.__init__` no longer builds agents (`_initialize_agents` runs on the first `get_agent`); `app/departments/__init__.py` gains `DEPARTMENT_CLASSES`, `list_departments()`, `get_department()` (case/space-insensitive) with cached instances. `app/api/departments_api.py` mounted in `main.py`: `GET /api/departments` → 13 rows `{name, manager, specialists[]}`; `POST /api/departments/{name}/execute {task, role?, require_json?}` → manager unless `role`, publishes `department_task`, 404 for an unknown department **and** for a role outside it, 502 on `LLMUnavailable`, `simulated` carried. `tests/test_departments_api.py` 9 tests. Live: `POST /api/departments/Research/execute` → `ResearchManager`, `simulated: false` |
| v9 E2 Departments tab | ✅ done | `pages/Departments.tsx`: one card per department (manager + specialist chips), an "Ask …" box with a manager/specialist picker, the answer rendered through `components/Markdown` with a `⚠ SIMULATED` badge. Sidebar entry **Departments** under Agents (`TabId` + `App.tsx`); `tsc` + `vite build` green |
| v9 E3 workflow steps may name a department | ✅ done | `core/workflow.py` `_department_names()` (lazy import, so `core` still stands alone) lets `validate` accept a department name; `runtime/workflow.py` `_resolve_agent` looks the role up in the registry first and falls back to the department's manager. `tests/test_departments_api.py::test_a_workflow_step_may_name_a_department`: a step with `"agent_role": "Research"` registers and resolves to `ResearchManager` |
| v9 E4 every persona has a department | ✅ done | New `app/departments/creative.py` (`CreativeDirectorAgent`); Research/Writing/Production/Publishing/Knowledge specialist lists extended to take in the personas the old `"X Department"` namespaces held. `GET /api/departments` → 13 departments, **105 members** (6+5+16+11+12+12+6+6+12+6+6+6+1). `tests/test_departments_api.py::test_every_persona_belongs_to_exactly_one_department` asserts no duplicates **and** `union(list_agents()) == agent_registry.agents.keys()`. Suite 160 passed |
| v9 F1 model tiers + `llamacpp` provider | ✅ done | Frontmatter `model_tier` → `AgentDefinition.model_tier` (validated to `fast`/`strong`, default `strong`); `"fast"` stamped on the 7 cheap personas (`TagGenerator`, `TitleGenerator`, `DescriptionWriter`, `MetadataOptimizer`, `PublishingChecklist`, `TaxonomyManager`, `CitationArchivist`). `integrations/llm.py` gains the `llamacpp` provider (OpenAI-compatible, `LLAMACPP_URL`, default `http://127.0.0.1:8089/v1`, 180 s timeout), `FALLBACK_ORDER = [gemini, openai, lm_studio, llamacpp]`, `DEFAULT_TIERS` (`fast → llamacpp`), and `_provider_order(tier)` / `_model_for(provider, tier)`; `generate_text`/`generate_chat` take `tier=` and `temperature=`; `BaseAgent` passes its own. `SettingsSchema` gains `llamacpp_url`, `llamacpp_model`, `tiers`; `Settings.tsx` now edits every model name, the two tiers and `owner_channel_ids`. `tests/test_llm.py` 9 tests: fast tier hits `127.0.0.1:8089` with the tier's model, falls through to LM Studio when 8089 refuses, order is tier-then-selection, tiers round-trip through `/api/settings` (against a tmp config file — the real `backend/config/config.json` hash is unchanged across the suite) |
| v9 F2 temperature per persona | ✅ done | Frontmatter `temperature` → `AgentDefinition.temperature` (clamped 0–2, default 0.7) → the provider payload. `HorrorWriter` set to 0.9, `FactChecker` to 0.2; `tests/test_llm.py::test_persona_temperature_reaches_the_provider_payload` asserts `post(...).json["temperature"]` is 0.9 / 0.2, and `ScriptWriter` (no frontmatter value) is 0.7. `/api/agents` rows now carry `model_tier` and `temperature`. Suite 169 passed; `tsc` + `vite build` green |
| v9 G1 docs + ECOSYSTEM contract | ✅ done | `AGENTS.md`: 105 personas / 13 departments, the merge note, `model_tier` + `temperature` rows in the frontmatter table, new "Model tiers" and "Departments" sections, delegation rewritten (`process_agent_invocations` in `services/studio_chat.py`, records, event, cap 3, depth 1, `## Delegated:` in step assets) — `grep -n "115\|19 depart" AGENTS.md` now only matches the "115 → 105, 19 → 13" note. `ARCHITECTURE.md`: dated v9, module table re-measured, departments + `Markdown.tsx` listed, request path and provider fallback show the tier hop, an Events section for `agent_invoked`/`department_task`, the research-dossier rule, and the stale "classified by string-matching" paragraph corrected (`classify_error` has been type-based since v5). `README.md`: 105 personas, 169 tests, a "The staff (v9)" section, model-tier row in Configuration. `ECOSYSTEM.md`: `LLAMACPP_URL`/`LLAMACPP_MODEL` rows, `studio_departments` + `studio_ask_department` tool rows, honest `studio_discover_topics` note, both new SSE frames with their payloads. `diff ECOSYSTEM.md ../dexter/ECOSYSTEM.md` → empty (0.4 still holds) |
| v6 S1 video intelligence (**J3**) | ✅ done | `services/video_intel.py` + `api/video_intel_api.py` + `pages/Analyze.tsx` (sidebar tab). Live 2026-09-06 on a public video: 5.5 s, captions 487 words (en, manual), 3 replay peaks with transcript, baseline 30 uploads (median 84.5K → outlier 21450×), LM Studio had no model loaded so the result was the labelled numbers-only read; `tests/test_video_intel.py` 6 tests (parsing, metrics, model JSON incl. fenced, brand guide in the prompt, cache, no-model fallback, channel outliers, 400/502/404); suite 143 passed; `tsc` + `vite build` green |

## v10 — the Studio can be closed from outside, and asks before LM Studio loads

Cross-repo context: `Buzzcaf_Media/GUARDIAN_PLAN.md` §6 (S1). Sentinel is the
machine's guardian of VRAM and RAM; the Studio never loads a model itself, but
it was the one app with no way to be shut down cleanly and the one whose
`lm_studio` provider could make LM Studio JIT-load several GB unattended.

| Item | Status | Evidence |
|---|---|---|
| v10 S1a `POST /api/studio/shutdown` | ✅ done | `app/api/studio_api.py`: loopback-only (`_is_loopback` parses the request's client host with `ipaddress`; a *hostname* never passes, so `0.0.0.0` binding later cannot turn "quit" into a network call), answers `{"ok": true, "message": …}` and then exits in a FastAPI background task. The exit reuses the window's own close path — `webview.window.destroy()` runs `on_closing` (geometry saved, Vite stopped), `webview.start()` returns, `main()` falls off the end, and the `atexit` hook registered at `desktop_app.py:503` removes `backend/config/studio.runtime.json`. Headless (uvicorn, no window) clears the record via `desktop_app._clear_runtime` and exits. Factored as `_shutdown_process()` so tests drive the route without ending the interpreter. `tests/test_studio_shutdown.py` 6 route tests |
| v10 S1b `integrations/sentinel_client.py` | ✅ done | Copy of Dexter's `backend/sentinel_client.py` (sha256 `34a121a1…`, 17 992 bytes, taken 2026-09-10). `diff` against the source shows **four lines** plus the module docstring: the logger name (`dexter.sentinel` → `buzzcaf_ai.sentinel`), one word in a docstring, and the `client()` factory building `SentinelClient(client="buzzcaf")` so the shared instance speaks as the Studio. Re-sync by copying the file over and re-applying those two edits. `query()` came from Dexter's own module and already has the required shape: `{"Query": {"client": "buzzcaf", "mib": …, "ram_mib": …}}` in, the `Reserved` shape back, and `(True, {"absent": true, …})` when the service is older than protocol v2 or absent |
| v10 S1c LM Studio asks first | ✅ done | `integrations/llm.py` `LLMService._sentinel_allows(provider, tier)` runs before every local-provider attempt in **both** `generate_text` and `generate_chat`. Only `lm_studio` is gated: `llamacpp` is buzzcode's engine, which reserves for itself before it starts, and asking again would book the same 12 GB twice. A refusal is logged with the reason and the blockers and the loop `continue`s to the next provider in `FALLBACK_ORDER` — it is a routing decision, not an error. An absent, older, or unreachable Sentinel answers "granted", i.e. exactly the pre-Sentinel behaviour. The size asked for comes from `estimate_model_mib()`, which reads a parameter count out of the model id (`meta-llama-3-8b-instruct` → 6144 MiB, `gpt-oss-20b` → 13 824) and falls back to `LM_STUDIO_DEFAULT_MIB = 6144` when the id says nothing useful — the Studio cannot measure a model it never loads. `tests/test_studio_shutdown.py` 11 gate/estimate tests |

**Verification.** `cd backend && python -m pytest tests -q` → **186 passed**
(169 before this work, + 17 new). No existing test changed.

**Not verified:** the Studio window was not launched, so the real
`window.destroy()` → `atexit` → `studio.runtime.json` removed chain rests on
reading `desktop_app.py`, not on watching it happen; the tests prove the route,
the loopback rule and that the exit function is reached. The live `Query` round
trip was not made either — the installed Sentinel is a v1 build with no `Query`,
which the client answers as "absent, granted", so the only live-observable
outcome today is "nothing changes".

`ECOSYSTEM.md` was deliberately **not** edited: the Dexter agent owns that file
and copies it across.

## v5 acceptance bar (Studio rows; the rest are in `dexter/PROGRESS.md`)

| # | Felt experience | Status | Evidence |
|---|---|---|---|
| I2 | One click opens the Studio | ✅ | `Buzzcaf Studio.vbs` → `[Desktop] Window opening after 3.7s`, no new `conhost`; `/health` → `app:"buzzcaf"` |
| I11 | Studio chat is the front door | ✅ | landing tab `studio_chat`; toasts for every failure; `grep -n "alert(\|confirm(\| fetch(" src` empty |
| I12 | Nothing lies | ✅ | fabricated projects/topics/video ideas removed; `simulated: true` travels to the UI badge; `list_brands` returns `[]` on error |
| I13 | Reversible | ✅ | `git tag -l` → `pre-v5` |

## v9 acceptance bar (felt experience)

Live rows were observed on 2026-09-09 against a real provider: Ollama
`qwen2.5:3b` on `127.0.0.1:11434/v1` reached through the OpenAI-compatible
`lm_studio` provider (codeBuzz's llama-server on 8089 was not running).

| # | Felt experience | Status | Evidence |
|---|---|---|---|
| K1 | Every persona is one click away | ✅ | Persona picker over all 105, grouped by department. Live: `FactChecker` on Beyond3Baje, `simulated: false`, answered as a fact checker (see A3) |
| K2 | I can see who did what | ✅ | Live: the Beyond3Baje strategist emitted `[INVOKE_AGENT: FactChecker]`, the Studio ran it and returned `invocations: [{agent: "FactChecker", task: "Verify in one line whether Bhangarh Fort has documented 1923…", simulated: false, output: "…I would check local historical archives and early 20th-century n…"}]`; one `agent_invoked` frame per honoured tag on `/api/studio/events`; cards render under the reply |
| K3 | I can read the script in the app | ✅ | Projects asset panel; `GET /api/projects/2026-07-22_Beyond3Baje_TestLiveLogger/asset/script` → 200 and the text renders through `components/Markdown` |
| K4 | Nothing lies, still | ✅ | `grep -rn "Information gathered from research" backend` → no match; research sections the model did not produce are absent and listed in `simulated_sections`; discover carries `source` and the UI badges the seed list; the invented `inventory_counts` and `status: "Active & Idle"` are gone |
| K5 | Departments answer | ✅ | Live: `POST /api/departments/Research/execute {"task": "Name two source types…"}` → `agent: "ResearchManager"`, `simulated: false`, *"I would check official government archives and old newspaper archives."* |
| K6 | Cheap work goes to the local model | ⚠ partial | The `fast` tier resolves to `llamacpp` and is tried first — proven in `tests/test_llm.py` against a mocked `127.0.0.1:8089` (and it falls through to LM Studio when that port refuses). **Not yet observed against the real llama-server**: 8089 was down for this session, so start codeBuzz's engine and re-check before calling this closed |

## 2026-09-10 - Ports: the Studio joins the shared ledger (GUARDIAN_PLAN section 11, P2)

- `backend/integrations/buzzcaf_ports.py` is a byte-identical copy of
  `dexter/backend/buzzcaf_ports.py` (sha256 `51649b81...`, 18 243 bytes).
  Never edit it here; re-copy Dexter's.
- `desktop_app.py` keeps `studio.runtime.json` **and** publishes to
  `%LOCALAPPDATA%\Buzzcaf\ports.json`. Port choice, dev-port choice and the
  attach-instead-of-duplicate check now go through the helper
  (`pick_port` / `discover`); the hand-rolled `_port_is_free` is gone, and the
  span constant comes from `buzzcaf_ports.SPAN`. `--dev` republishes with
  `extra: {vite}`. Both records are removed on exit; a `READY port=N` line tells
  a launcher which port to open.
- `app/main.py` gained the headless entry Dexter's `buzzcaf_client` documents:
  `cd backend && python -m app.main` picks its own port, publishes, prints
  `READY port=N` and withdraws. `/health` now answers
  `{"status":"ok","app":"buzzcaf","version":...,"port":n,"pid":n}`.
- Evidence: `cd backend && python -m pytest tests -q` -> **195 passed**
  (186 before, +9 in `tests/test_ports.py`). Live, headless, real ledger:
  preferred 8099 free -> bound 8099, entry
  `{"port":8099,"pid":27256,"health":"http://127.0.0.1:8099/health"}`, health
  `{"status":"ok","app":"buzzcaf","version":"0.5.0","port":8099,"pid":27256}`;
  with a dummy listener on 8099 -> bound **8100**, same shape; Ctrl-C left the
  ledger without a `buzzcaf` key both times.
- The Studio has no link to Dexter in code (`grep -rn 8098` / `DEXTER_URL`
  across `backend/`, `frontend/`, `scripts/` finds only the ECOSYSTEM.md table),
  so there was nothing to convert to `discover()`. `ECOSYSTEM.md` untouched:
  the Dexter agent copies it over.

## 2026-09-09

- Plan v9 (staff activation) worked A → C → D → B → E → F → G. 115 → 105
  personas, 19 → 13 departments, `pytest tests -q` 144 → **169 passed**,
  `tsc` + `vite build` green after every phase. Nothing committed: the whole
  change sits in the working tree.

## 2026-09-06

- Plan v5 adopted. The repair pass recorded in `IMPROVEMENTS.md` (paths,
  `json` import, masked settings, labelled fallbacks) was committed as four
  commits and tagged `pre-v5` so every v5 change is reversible.

# Workflow Principles

Every workflow must define: - Trigger - Inputs - Required agents -
Outputs - Validation - Completion criteria - Archive location

Every workflow is deterministic wherever possible.

# docs

The current documentation is `README.md`, `ARCHITECTURE.md`, `AGENTS.md`, `PLAN.md`, `PROGRESS.md` and `ECOSYSTEM.md` in the repository root.

`legacy/` holds 185 earlier documents that described a design most of which was never implemented (the stub modules were deleted in v5). They are kept for history only; do not build on them.
